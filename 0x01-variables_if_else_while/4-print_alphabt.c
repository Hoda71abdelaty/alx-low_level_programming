#include <stdio.h>
/**
 * main - Entry point
 * Description: print lowercase alphabet
 * Return: Always 0 (success)
 */
int main(void)
{
	char let = 'a';

	/*print a -z*/
	while (let <= 'z', let != q, e)
	{
		putchar(let);
		let++;
	}
	return (0)
}
